package definitions;

enum Boolean {
    True, False
}

port Electrical {

}

port PressureInfo {
    Properties:
        pressurized = {True, False};
}

port Mechanical {
    Properties:
        powered = {True, False};
}

port Fluid {
    Properties:
        running = {True, False};
}

